# Default values for horizon.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

replicaCount: 1

mongodb:
  uri:

image: files.evertrust.fr:5051/horizon:2.0

imageCredentials:
  registry: files.evertrust.fr:5051
  username:
  password:

play:
  allowed_hosts: '["."]'

horizon:
  license:

vault:
  type: ssv
  password: evertrust
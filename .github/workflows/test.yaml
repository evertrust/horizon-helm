on:
  push:
    branches:
      - '**'


jobs:
  test:
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v4


      - name: Start minikube
        uses: medyagh/setup-minikube@latest
        with:
          start-args: '--force'
          driver: 'none'

      - uses: azure/setup-helm@v4.2.0

      - name: Prepare Horizon environment
        run: kubectl create namespace horizon

      - name: Create license file
        run: |
          echo ${{secrets.LICENSE}} > license.lic
          kubectl create secret generic horizon-license \
            --from-file=license="license.lic" \
            --namespace horizon

      - name: Create values file
        run: |
          cat <<EOF > override-values.yaml
          image:
            pullSecrets:
              - evertrust-registry
          
          license:
            secretName: horizon-license
            secretKey: license
          
          allowedHosts:
            - localhost:9443
            - horizon-test-connection:9000
          EOF

      - name: Install Horizon
        run: |
          helm repo add bitnami https://charts.bitnami.com/bitnami
          helm dependency build
          helm install --namespace horizon --values override-values.yaml horizon .
          

      - name: Test Horizon
        run: helm test --namespace horizon horizon